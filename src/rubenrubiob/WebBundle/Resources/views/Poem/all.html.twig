<html>
<head>
    <title>List of Poems</title>
</head>
<body>
    <h1>List of Poems</h1>

    {% if poems | length > 0 %}
        <ul>
            {% for poem in poems %}
                <li>
                    <ul>
                        <li class="poem-title">
                            Title: {{ poem.title }}
                        </li>
                        <li class="author-name">
                            Author: {{ poem.author.name }}
                        </li>
                        <li class="i-should-not-be-here">
                            Foo: bar
                        </li>
                    </ul>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>No poems found</p>
    {% endif %}
</body>
</html>